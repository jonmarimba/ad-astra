**GReptile**

@ghost

Hello?! GReptile dude.

@ghost. you missed this one TWICE. Pay attention

**GhOST reply, 2026-08-10 17:18:**

Greptile is an AI code-review tool: it reviews PRs with full-codebase context (not just the diff), leaves inline comments, and learns your codebase's patterns over time; it also does codebase Q&ampA/search by building a graph of the repo. Aimed at teams that want an automated first-pass PR review.

Paid SaaS (per-seat + usage), self-serve and enterprise tiers. For you: it could auto-review your Swift/iOS PRs. One real caveat — it indexes your source on their infrastructure, so think before pointing it at Maharam/client repos; a personal repo is the safe trial. Web-search budget this session is spent, so verify current pricing/terms before committing.

Want me to trial it on a non-client repo?

@ghost stop bitching about web search being exhausted. See what is has to say about kicker?

**GhOST reply, 2026-08-18:**

This was researched and the verdict is recorded in the canonical tech-to-try list, which now lives in @astra and is symlinked into the Obsidian vault at *tech-to-try.md*. Replying here because the research landed but this note was never marked answered, which is why it kept resurfacing in the pending queue.

You are right that this was missed twice, and the reason is worth saying: the work was done and the note was never answered, so the queue could not tell the difference between finished and ignored. Ten notes were sitting in exactly that state.

**Greptile** (greptile.com) — AI PR review with full-codebase context. The caveat that decides where it can be used: it indexes your source on their infrastructure, so it is fine for personal repos and not for Maharam or any client work. Your open question on it — what it says about kicker specifically — is still open, and it is a question only a trial run answers.